# 홀/짝을 입력하시오 
# 나:홀
# 컴:홀
# 결과:승리
from random import random

player = input("나")
com = [0.5]

for i in range(100):
    rnd = int(random() * len(com))
    a = arr45[0]
    b = arr45[rnd]
    arr45[0] = b
    arr45[rnd] = a